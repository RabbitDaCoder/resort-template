# Zoho SMTP credentials
SMTP_HOST=smtp.zoho.com
SMTP_PORT=587
PROJECT_ID=DSCV-S4ML0R7X
SMTP_USER=reservations@discoverysamal-resort.org
SMTP_PASS=your_zoho_app_password

# Sender display name override (optional)
# EMAIL_FROM="Discovery Samal Resort" <reservations@discoverysamal-resort.org>

# Shared secret — must match EMAIL_SERVICE_API_KEY in backend .env
EMAIL_API_KEY=generate_a_strong_random_secret_here

# Resort contact info (used in email footer)
RESORT_PHONE=+639757455801
RESORT_EMAIL=reservations@discoverysamal-resort.org
FACEBOOK_PAGE_URL=https://www.facebook.com/share/14XkPrgvyr3/?mibextid=wwXIfr

# Admin email (receives new booking + payment alert notifications)
ADMIN_EMAIL=reservations@discoverysamal-resort.org

# Frontend URL (for payment links in emails)
CLIENT_URL=https://www.discoverysamal-resort.org

# Backend API URL — used to fetch live social links (phone, Facebook) managed by admin
BACKEND_URL=https://your-backend.onrender.com
